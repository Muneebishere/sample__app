    <header class="navbar navbar-fixed-top">
        <div class="container">
          <%= link_to "sample app", root_path, id: "logo" %>
            <nav>
              <ul class="nav pull-right">
              <% if signed_in? %>
                  <li id="fat-menu" class="dropdown horizontal">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      Account <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                      <li class="horizontal"><%= link_to "Profile", current_user %></li>
                      <li class="horizontal"><%= link_to "Settings", edit_user_path(current_user) %></li>
                      <li class="horizontal">
                        <%= link_to "Sign out", signout_path, method: "delete" %>
                      </li>
                    </ul>
                  </li>
                  <li class="horizontal"><%= link_to "Users", users_path %></li>
                  <% else %>
                    <li class="horizontal"><%= link_to "Sign in", signin_path %></li>
                  <% end %>
                <li class="horizontal"><%= link_to "Help", help_path %></li>
                <li class="horizontal"><%= link_to "Home", root_path %></li>
              </ul>
            </nav>
        </div>
    </header>
